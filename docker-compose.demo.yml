services:
  web:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:?set VITE_API_BASE_URL to the public HTTPS backend URL}
    ports:
      - "8080:8080"
    restart: unless-stopped
